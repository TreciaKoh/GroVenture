<%= form_for(@product) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<%=f.fields_for :product, @product do |e|%>
  <div class="field">
    <%= e.label :name %><br>
    <%= e.text_field :name %>
  </div>
<%end%>
<%=f.fields_for :product, @product do |e|%>
  <div class="field">
    <%= e.label :name %><br>
    <%= e.text_field :name %>
  </div>
<%end%>
<%=f.fields_for :product, @product do |e|%>
  <div class="field">
    <%= e.label :name %><br>
    <%= e.text_field :name %>
  </div>
<%end%>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
<!-- 
<%=form_tag('/products/create') do%>
<%for i in 0..2%>
<%=text_field_tag("name"+i.to_s)%>
<%end%>
<%=submit_tag("Submit")%>
<%end%> -->
